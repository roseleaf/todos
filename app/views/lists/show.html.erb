<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @list.name %>
</p>

<div>
  <b>Tasks:</b>
  <ul>
    <%= render @list.tasks %>
  </ul>
</div>

<%= render "tasks/form" %>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
